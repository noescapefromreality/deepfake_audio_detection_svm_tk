To compile, ensure you have the needed dependencies, then run "pyinstaller --one-file app.py" from this directory. It will create an app which uses the model trained on the lightweight version of the "In-the-Wild" dataset
